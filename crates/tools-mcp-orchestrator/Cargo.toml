[package]
name = "tools-mcp-orchestrator"
version = "0.1.0"
edition = "2024"

[dependencies]
async-trait = "0.1"
serde = { version = "1.0", features = ["derive"] }
serde_yml = "0.0.12"
toml = "0.8"
tools-mcp-core = { path = "../tools-mcp-core" }

[dev-dependencies]
tempfile = "3.12"
