[package]
name = "tools4a-core"
version = "0.1.0"
edition = "2024"

[dependencies]
async-trait = "0.1"
schemars = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_yml = "0.0.12"
toml = "0.8"

[dev-dependencies]
tempfile = "3.12"
